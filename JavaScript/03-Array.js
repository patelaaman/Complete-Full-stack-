// concat 

const initialArray = [1,2,4];
const secondArray = [4,5,3];
const final  = initialArray.concat(secondArray);
console.log(final)


// forEach()
function logThings(str){
    console.log(str);
}

initialArray.forEach(logThings)

// callbacks map , filter  , find , sort 
